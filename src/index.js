import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

// Încadrează componenta App în containerul app-container
ReactDOM.render(
  <div id="app-container">
    <App />
  </div>,
  document.getElementById('root')
);
